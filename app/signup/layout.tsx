export const metadata = {
  title: "Signup - Creia",
  description: "Create a Creia account to start collaborating in real-time.",
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
